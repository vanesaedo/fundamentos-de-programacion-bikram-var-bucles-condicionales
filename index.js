//-------ITERACIONES-------

//Variables: 1,2,3,4.

let variableSinValor;
let booleano1 = true;
let booleano2 = true;
const PI = 3.14;
const TAU = PI*2;

//Booleanos: 5,6,7.

let booleanoAnd = (booleano1 == booleano2);
let booleanoNot = !booleano1;
let booleanoMix0 = (booleano1 || booleano2) && (booleano1 && !booleano2);

let incrementarDesp = 2;
resultadoDesp = incrementarDesp++;

//Operadores: 8,9.

let incrementarAntes = 2;
resultadoAntes = ++2;

//Bucles: 10,11,12.

let contarHasta10_2 = 0;
for (contarHasta10 = 0; contarHasta10 <= 10; contarHasta10++){
   
}

let postI = 0;
let postJ = 0;
for (let i = 0; i <=10; i++){
    postI = postI + postJ;
}

let sumaPares = 0;
for (let i = 0; i < 10; i++){
    if (i%2 == 0){
        sumaPares = sumaPares + i;
    }
}

//----------ITERACIONES: proyecto individual european_castle

//variables: 13, 14, 15

let variableValorNumerico = 0;
const MiNombre = 'Vanesa';
const MiNumeroFav = 4;

//Booleanos: 16, 17,18,19

const booleano0r = booleano1 || booleano2;
booleanoMix1 = (booleano1 && (TAU/2 == PI)) || (variableValorNumerico >= MiNumeroFav);
seisNoEsNueve = 6 === 9;
booleanoMix_2 = (variableValorNumerico > 0) || (MiNumeroFav * TAU);

//Operadores: 20, 21, 22, 23.

let valorSuma = MiNumeroFav + variableValorNumerico;
let valorResta = MiNumeroFav - variableValorNumerico;
let valorMultiplicacion = MiNumeroFav * variableValorNumerico;
let valorDivision = MiNumeroFav / 3;

//Bucles: 24, 25, 26.
let contarHasta10 = 0;
while (contarHasta10 < 10){
    contarHasta10++;
};





